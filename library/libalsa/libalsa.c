#include "libalsa.h"
/*
struct sound_callback ctx;

CRT_FUNCDECL(int, librecord_wave,(sound_callback_threads libthreadproc, struct sysnode_proc_t *psys)){
    ctx.callback = libthreadproc;

    ctx.libmic_init = libmic_init;
    ctx.libmic_exit = libmic_exit;
    ctx.libmic_surx = libmic_surw;

    if(!libmic_run_state || libmic_run_state <= 0) ctx.libmic_init(psys);

    run("b.wav", &ctx);

    ctx.libmic_exit(psys);
    return 0;
}
*/

